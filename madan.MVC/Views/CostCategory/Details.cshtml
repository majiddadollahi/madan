@model madan.MVC.Data.Models.CostCategory

@{
    ViewData["Title"] = "جزئیات دسته‌بندی هزینه";
}

<h1>جزئیات دسته‌بندی هزینه</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            نام
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            وضعیت
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IsActive)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">ویرایش</a> |
    <a asp-action="Index" class="btn btn-secondary">بازگشت به لیست</a>
</div> 